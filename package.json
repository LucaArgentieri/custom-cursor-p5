{
  "name": "custom-cursor-p5-monorepo",
  "private": true,
  "main": "index.js",
  "workspaces": {
    "packages": [
      "lib/*",
      "site/*"
    ]
  },
  "scripts": {
    "deploy": "git subtree push --prefix site/dist origin gh-pages"
  }
}
