{
  "name": "custom-cursor-p5-site",
  "private": false,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "@types/p5": "^1.4.2",
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  },
  "dependencies": {
    "custom-cursor-p5": "*",
    "p5": "^1.4.2"
  }
}
